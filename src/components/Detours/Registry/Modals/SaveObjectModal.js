// import {classic} from "rt-design";

export const AddDetour = () => operationOnServer('add');
export const EditDetour = () => operationOnServer('add');

// const {Modal, FormBody, Title, Input, DatePicker}=classic
const operationOnServer = (type) => {
	return <div>1</div>;
};
